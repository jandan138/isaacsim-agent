\begin{table*}[!t]
  \centering
  \caption{Recorded short-forward navigation traces under a shared declared tool list. \texttt{P0} can emit an undeclared action, \texttt{P1} binds the emission to a dispatchable typed call, and \texttt{P2} adds only a brief planner-side \texttt{self\_check}.}
  \label{tab:contract-interface-examples}
  \footnotesize
  \setlength{\tabcolsep}{4pt}
  \renewcommand{\arraystretch}{1.12}
  \colorbox{ralPanel}{%
    \parbox{0.968\textwidth}{%
      \raggedright
      \textbf{Shared declared tool list in every request: }
      \texttt{get\_robot\_state} \hspace{0.35em}\textbar\hspace{0.35em}
      \texttt{get\_goal\_state} \hspace{0.35em}\textbar\hspace{0.35em}
      \texttt{navigate\_to(target\_pose)}\\[-1pt]
      {\scriptsize\textcolor{ralMuted}{binding strength: \texttt{P0} $\rightarrow$ \texttt{P1} $\rightarrow$ \texttt{P2}}}
    }%
  }\\[4pt]
  \begin{tabularx}{0.982\textwidth}{@{}>{\raggedright\arraybackslash\bfseries}p{0.15\textwidth} Y Y Y@{}}
    \toprule
    \rowcolor{ralPanel}
    & \textbf{P0 --- Free JSON} & \textbf{P1 --- Typed call} & \textbf{P2 --- Typed call + self-check} \\
    \midrule
    Cue
    & minimal JSON action
    & choose one declared tool + args
    & P1 + brief self-check \\
    \addlinespace[2pt]
    Example emission
    & \parbox[t]{\linewidth}{\ttfamily\scriptsize tool\_name: move\_to\_goal\\arguments: \{\}}
    & \parbox[t]{\linewidth}{\ttfamily\scriptsize tool\_name: navigate\_to\\target\_pose: (1.0, 0.0, 0.0)}
    & \parbox[t]{\linewidth}{\ttfamily\scriptsize tool\_name: navigate\_to\\target\_pose: (1.0, 0.0, 0.0)\\self\_check: target\_pose matches\\the configured goal pose} \\
    \addlinespace[2pt]
    Runtime effect
    & may emit undeclared tool; not dispatchable
    & declared callable + required args; dispatchable
    & same dispatch surface as P1 + planner-side verification \\
    \bottomrule
  \end{tabularx}
\end{table*}
